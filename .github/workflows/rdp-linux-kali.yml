
name: Kali-RDP-Mobile

on:
  workflow_dispatch:
    inputs:
      duration:
        description: 'Runtime in minutes (max 360)'
        required: false
        default: '180'

jobs:
  kali-rdp:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: Setup Ubuntu Desktop with Security Tools
        run: |
          set -e
          echo "🔧 Starting installation process..."
          
          # Add necessary repositories
          sudo add-apt-repository universe -y
          sudo add-apt-repository multiverse -y
          sudo apt-get update
          
          # Install desktop environment
          echo "📀 Installing desktop environment..."
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y \
            xfce4 xfce4-goodies \
            xrdp \
            firefox \
            nano vim curl wget unzip git ruby ruby-dev

          # Install security tools that ARE available
          echo "🛠️ Installing security tools..."
          sudo apt-get install -y \
            nmap nikto sqlmap gobuster dirb ffuf \
            python3 python3-pip python3-venv \
            john hashcat hydra wireshark tcpdump netcat

          # Create user
          sudo useradd -m -s /bin/bash kali 2>/dev/null || true
          echo "kali:kali123456" | sudo chpasswd
          sudo usermod -aG sudo kali

          # Configure xrdp
          sudo sed -i 's/port=3389/port=3390/g' /etc/xrdp/xrdp.ini
          echo "xfce4-session" | sudo tee /etc/xrdp/startwm.sh
          sudo chmod +x /etc/xrdp/startwm.sh

          # Start RDP service
          sudo systemctl enable xrdp
          sudo systemctl start xrdp

          echo "✅ Desktop environment ready!"

      - name: Install Metasploit
        run: |
          echo "📥 Installing Metasploit..."
          # Install dependencies
          sudo apt-get install -y build-essential libpq-dev postgresql
          
          # Download and install Metasploit
          wget -q https://raw.githubusercontent.com/rapid7/metasploit-omnibus/master/config/templates/metasploit-framework-wrappers/msfupdate.erb
          sudo ruby msfupdate.erb
          echo "✅ Metasploit installed"

      - name: Install Feroxbuster
        run: |
          wget -q https://github.com/epi052/feroxbuster/releases/download/2.10.0/feroxbuster-v2.10.0-linux-x86_64.zip
          unzip -o feroxbuster-v2.10.0-linux-x86_64.zip
          sudo mv feroxbuster /usr/local/bin/
          sudo chmod +x /usr/local/bin/feroxbuster
          echo "✅ Feroxbuster installed"

      - name: Install Additional Python Tools
        run: |
          pip3 install requests bs4 scrapy
          echo "✅ Python tools installed"

      - name: Verify Installation
        run: |
          echo "🔍 Verifying installed tools..."
          which nmap && echo "✅ nmap"
          which sqlmap && echo "✅ sqlmap" 
          which msfconsole && echo "✅ metasploit"
          which feroxbuster && echo "✅ feroxbuster"
          echo "✅ All critical tools installed!"

      - name: Setup Playit Tunnel (RDP Access)
        env:
          PLAYIT_KEY: ${{ secrets.PLAYIT_KEY }}
        run: |
          echo "🌐 Setting up Playit tunnel..."
          wget -q https://github.com/playit-cloud/playit-agent/releases/download/v0.15.26/playit-linux-x86_64-v0.15.26.tar.gz
          tar -xzf playit-linux-x86_64-v0.15.26.tar.gz
          sudo mv playit /usr/local/bin/
          sudo chmod +x /usr/local/bin/playit

          nohup playit --secret "$PLAYIT_KEY" --tunnel 3390 > playit.log 2>&1 &
          sleep 25

          TUNNEL_URL=$(grep -o 'https://[^ ]*\.ply\.gg' playit.log | head -1)
          echo "🎯 RDP Connection Details:" 
          echo "Address: $TUNNEL_URL"
          echo "Port: 3390"
          echo "Username: kali"
          echo "Password: kali123456"
          echo "📱 Use Windows RDP app on your phone to connect!"

      - name: Keep Alive with Countdown
        run: |
          DURATION=${{ github.event.inputs.duration || 180 }}
          echo "🕐 RDP session active for $DURATION minutes"
          echo "🔧 Tools available: nmap, metasploit, sqlmap, burpsuite, wireshark, etc."
          
          MINUTES=$DURATION
          for ((i=MINUTES; i>0; i--)); do
            echo "⏳ Time remaining: ${i} minutes"
            sleep 60
          done
          echo "❌ Session ending now"          
          echo "🎯 RDP Connection Details:" 
          echo "Address: $TUNNEL_URL"
          echo "Port: 3390"
          echo "Username: kali"
          echo "Password: kali123456"
          echo "📱 Use Windows RDP app on your phone to connect!"

      - name: Keep Alive with Countdown
        run: |
          # Use the input duration or default to 180 minutes
          DURATION=${{ github.event.inputs.duration || 180 }}
          echo "🕐 RDP session active for $DURATION minutes"
          echo "⏰ Session started at: $(date)"
          echo "🔌 Will auto-terminate at: $(date -d '+$DURATION minutes')"
          echo ""
          echo "🔧 Available tools: nmap, nikto, sqlmap, metasploit, gobuster, ffuf, feroxbuster, john, hashcat, hydra"
          echo ""
          
          # Countdown timer
          MINUTES=$DURATION
          for ((i=MINUTES; i>0; i--)); do
            echo "⏳ Time remaining: ${i} minutes"
            sleep 60
          done
          echo "❌ Session ending now"
