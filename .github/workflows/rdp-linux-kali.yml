
name: Kali-RDP-Mobile-Manual

on:
  workflow_dispatch:
    inputs:
      duration:
        description: 'Runtime in minutes (max 360)'
        required: false
        default: '180'

jobs:
  kali-rdp:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: Checkout repository (includes manual playit binary)
        uses: actions/checkout@v4

      - name: Setup Ubuntu Desktop with Security Tools
        run: |
          set -e
          echo "🔧 Starting installation process..."
          
          sudo add-apt-repository universe -y
          sudo add-apt-repository multiverse -y
          sudo apt-get update
          
          echo "📀 Installing desktop environment..."
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y \
            xfce4 xfce4-goodies \
            xrdp \
            firefox \
            nano vim curl wget unzip git

          echo "🛠️ Installing security tools..."
          sudo apt-get install -y \
            nmap nikto sqlmap gobuster dirb ffuf \
            python3 python3-pip python3-venv \
            john hashcat hydra wireshark tcpdump \
            netcat-openbsd

          # Create user
          sudo useradd -m -s /bin/bash kali 2>/dev/null || true
          echo "kali:kali123456" | sudo chpasswd
          sudo usermod -aG sudo kali

          # Configure xrdp
          sudo sed -i 's/port=3389/port=3390/g' /etc/xrdp/xrdp.ini
          echo "xfce4-session" | sudo tee /etc/xrdp/startwm.sh
          sudo chmod +x /etc/xrdp/startwm.sh

          # Start RDP service
          sudo systemctl enable xrdp
          sudo systemctl start xrdp

          echo "✅ Desktop environment ready!"

      - name: Install Additional Python Tools
        run: |
          pip3 install requests bs4 scrapy
          echo "✅ Python tools installed"

      - name: Setup Playit Tunnel (USING MANUAL BINARY - LATEST VERSION)
        env:
          PLAYIT_KEY: ${{ secrets.PLAYIT_KEY }}
        run: |
          echo "🌐 Setting up Playit tunnel using manually uploaded binary..."
          
          # Use the manually uploaded binary
          echo "📁 Using local playit binary..."
          cp ./binaries/playit ./playit
          
          # Verify it's a real binary
          echo "🔍 Verifying binary..."
          file playit
          ls -lh playit
          echo "📏 File size: $(wc -c < playit) bytes"
          
          if ! file playit | grep -q "ELF"; then
            echo "❌ ERROR: File is not a valid binary!"
            echo "💡 Make sure you downloaded: playit-linux-amd64"
            echo "💡 From: https://github.com/playit-cloud/playit-agent/releases"
            exit 1
          fi
          
          # Make executable and install
          chmod +x playit
          sudo mv playit /usr/local/bin/
          echo "✅ Playit installed successfully"

          # Start tunnel
          echo "🚀 Starting tunnel..."
          /usr/local/bin/playit --secret "$PLAYIT_KEY" --tunnel 3390 2>&1 | tee playit.log &
          TUNNEL_PID=$!
          
          # Wait for tunnel to establish
          echo "⏳ Waiting for tunnel to establish (up to 90 seconds)..."
          for i in {1..90}; do
            if grep -q "https://.*\.ply\.gg" playit.log; then
              echo "✅ Tunnel established after $i seconds!"
              break
            fi
            if [ $i -eq 30 ]; then
              echo "⏰ Still waiting... (30 seconds)"
            fi
            if [ $i -eq 60 ]; then
              echo "⏰ Still waiting... (60 seconds)"
            fi
            sleep 1
          done
          
          # Extract connection details
          ADDRESS=$(grep -oP 'https://\K[\w\-\.]+\.ply\.gg:\d+' playit.log | head -1)
          
          if [ -n "$ADDRESS" ]; then
            echo ""
            echo "🎯 🎯 🎯 RDP CONNECTION DETAILS 🎯 🎯 🎯"
            echo ">>> RDP ADDRESS: $ADDRESS <<<"
            echo "Username: kali"
            echo "Password: kali123456"
            echo ""
            echo "📱 In Windows RDP app:"
            echo "Computer: $(echo $ADDRESS | cut -d: -f1)"
            echo "Port: $(echo $ADDRESS | cut -d: -f2)"
            echo "🎯 🎯 🎯"
            
            # Show recent log for debugging
            echo "📋 Recent tunnel log:"
            tail -10 playit.log
          else
            echo "❌ No tunnel URL found after 90 seconds"
            echo "📋 Full log:"
            cat playit.log
            echo "❌ Tunnel failed - check your PLAYIT_KEY secret"
            exit 1
          fi

      - name: Keep Alive with Countdown
        run: |
          DURATION=${{ github.event.inputs.duration || 180 }}
          echo "🕐 RDP session active for $DURATION minutes"
          echo "🔧 Tools available: nmap, sqlmap, ffuf, gobuster, etc."
          
          MINUTES=$DURATION
          for ((i=MINUTES; i>0; i--)); do
            echo "⏳ Time remaining: ${i} minutes"
            sleep 60
          done
          echo "❌ Session ending now"
