
name: Kali Docker RDP

on: workflow_dispatch

jobs:
  kali-rdp:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    
    steps:
      - name: Start Kali Linux with RDP
        run: |
          echo "🚀 Starting Kali Linux in Docker..."
          docker run -d --name kali-rdp -p 3389:3389 kalilinux/kali-rolling
          echo "✅ Kali container started!"
          
      - name: Keep Alive
        run: |
          echo "⏰ RDP will be available for 2 hours"
          sleep 7200
