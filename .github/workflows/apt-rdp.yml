
name: APTRDP

on:
  workflow_dispatch:

jobs:
  setup-rdp-tunnel:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Install Playit
      shell: pwsh
      run: |
        Invoke-WebRequest -Uri "https://github.com/playit-cloud/playit-agent/releases/download/v0.15.26/playit-windows-x86_64-signed.exe" -OutFile "$env:USERPROFILE\playit.exe"

    - name: Configure RDP
      shell: pwsh
      env:
        PASS: ${{ secrets.RDP_PASS }}
      run: |
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
        Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 1
        Set-LocalUser -Name "runneradmin" -Password (ConvertTo-SecureString -AsPlainText $env:PASS -Force)

    - name: Install Essential Tools
      shell: pwsh
      run: |
        # Install 7-Zip
        Write-Host "Installing 7-Zip..."
        Invoke-WebRequest -Uri "https://www.7-zip.org/a/7z2408-x64.msi" -OutFile "$env:TEMP\7z.msi"
        Start-Process "msiexec" -ArgumentList "/i", "$env:TEMP\7z.msi", "/quiet", "/norestart" -Wait
        
        # Install VLC Portable
        Write-Host "Installing VLC Portable..."
        Invoke-WebRequest -Uri "https://download.videolan.org/pub/vlc/3.0.21/win64/vlc-3.0.21-win64.7z" -OutFile "$env:TEMP\vlc.7z"
        & "C:\Program Files\7-Zip\7z.exe" x "$env:TEMP\vlc.7z" -o"C:\Program Files\VLC" -y -bso0
        
        # Install Firefox
        Write-Host "Installing Firefox..."
        Invoke-WebRequest -Uri "https://download.mozilla.org/?product=firefox-latest&os=win64&lang=en-US" -OutFile "$env:TEMP\firefox.exe"
        Start-Process "$env:TEMP\firefox.exe" -ArgumentList "/S" -Wait
        
          # Install Lossless Cut - FIXED: Using the correct 7z portable version
        Write-Host "Installing Lossless Cut..."
        Invoke-WebRequest -Uri "https://github.com/mifi/lossless-cut/releases/download/v3.67.1/LosslessCut-win-x64.7z" -OutFile "$env:TEMP\LosslessCut.7z"
        & "C:\Program Files\7-Zip\7z.exe" x "$env:TEMP\LosslessCut.7z" -o"C:\Program Files\LosslessCut" -y -bso0
        
        Write-Host "All tools installed successfully!"

    - name: Install Media Tools (yt-dlp + ffmpeg)
      shell: pwsh
      run: |
        # Install yt-dlp
        Write-Host "Installing yt-dlp..."
        Invoke-WebRequest -Uri "https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp.exe" -OutFile "C:\Windows\System32\yt-dlp.exe"
        
        # Install ffmpeg - USING YOUR ORIGINAL WORKING METHOD
        Write-Host "Installing ffmpeg..."
        $fftmp = "$env:TEMP\ff"; mkdir $fftmp -Force >$null;
        Invoke-WebRequest -Uri "https://www.gyan.dev/ffmpeg/builds/ffmpeg-release-full.7z" -OutFile "$fftmp\ff.7z";
        & "C:\Program Files\7-Zip\7z.exe" x "$fftmp\ff.7z" -o"$fftmp" -y;
        Copy-Item "$fftmp\ffmpeg-*\bin\*.exe" "C:\Windows\System32\" -Force
        
        # Create desktop shortcuts
        $WshShell = New-Object -comObject WScript.Shell
        
        # FIXED: Lossless Cut shortcut now points to portable version
        $Shortcut = $WshShell.CreateShortcut("$env:USERPROFILE\Desktop\Lossless Cut.lnk")
        $Shortcut.TargetPath = "C:\Program Files\LosslessCut\LosslessCut.exe"
        $Shortcut.WorkingDirectory = "C:\Program Files\LosslessCut"
        $Shortcut.Save()
        
        $Shortcut = $WshShell.CreateShortcut("$env:USERPROFILE\Desktop\VLC Player.lnk")
        $Shortcut.TargetPath = "C:\Program Files\VLC\vlc.exe"
        $Shortcut.WorkingDirectory = "C:\Program Files\VLC"
        $Shortcut.Save()
        
        $Shortcut = $WshShell.CreateShortcut("$env:USERPROFILE\Desktop\Firefox.lnk")
        $Shortcut.TargetPath = "C:\Program Files\Mozilla Firefox\firefox.exe"
        $Shortcut.Save()

    - name: Create Download Script
      shell: pwsh
      run: |
        # Create download script on desktop
        $downloadScript = @'
        @echo off
        chcp 65001 >nul
        echo ===============================
        echo    YouTube Download Script
        echo ===============================
        echo.
        if "%~1"=="" (
            set /p "url=Enter YouTube URL: "
        ) else (
            set "url=%~1"
        )
        echo Downloading: %url%
        yt-dlp -N 8 -o "%USERPROFILE%\Downloads\%(title)s-%(height)sp.%(ext)s" "%url%"
        if %errorlevel% equ 0 (
            echo Download completed successfully!
        ) else (
            echo Download failed with error code %errorlevel%
        )
        pause
        '@
        $downloadScript | Out-File -FilePath "$env:USERPROFILE\Desktop\Download.bat" -Encoding ASCII

        # Also create a PowerShell version
        $psScript = @'
        # PowerShell Download Script
        Write-Host "YouTube Download Script" -ForegroundColor Green
        $url = Read-Host "Enter YouTube URL"
        Write-Host "Downloading: $url" -ForegroundColor Yellow
        & yt-dlp -N 8 -o "$env:USERPROFILE\Downloads\%(title)s-%(height)sp.%(ext)s" "$url"
        if ($LASTEXITCODE -eq 0) {
            Write-Host "Download completed successfully!" -ForegroundColor Green
        } else {
            Write-Host "Download failed with error code $LASTEXITCODE" -ForegroundColor Red
        }
        Read-Host "Press Enter to continue"
        '@
        $psScript | Out-File -FilePath "$env:USERPROFILE\Desktop\Download.ps1" -Encoding ASCII

    - name: Show tunnel address
      shell: pwsh
      env:
        PLAYIT_AUTH_KEY: ${{ secrets.PL }}
      run: |
        # Start playit in foreground
        $arg = '--secret', $env:PLAYIT_AUTH_KEY
        & "$env:USERPROFILE\playit.exe" $arg 2>&1 | Tee-Object -Variable out

        # Find the tunnel URL
        $m = $out | Select-String -Pattern 'https://([\w\-\.]+\.ply\.gg:\d+)' | Select-Object -First 1
        if ($m) {
            $addr = $m.Matches.Groups[1].Value
            Write-Host "`n>>> RDP ADDRESS: $addr <<<`n"
        } else {
            Write-Host "no tunnel URL found"
            exit 1
        }

    # ---------- keep alive ----------
    - name: Keep runner alive
      shell: pwsh
      run: Start-Sleep 21600        # 6 h
